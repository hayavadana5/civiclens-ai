# Base URL of the CivicLens backend. Falls back to mock data if unreachable.
VITE_API_URL=http://localhost:5000/api
# Set to "true" to skip network calls entirely and always use mock data.
VITE_USE_MOCK=false
